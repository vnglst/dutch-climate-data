name: Update Rainfall

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * 5" # Runs at 12:00 PM every Friday

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"
      - name: Install Poetry
        run: |
          pipx install poetry==1.2.0
      - name: Install Dependencies
        run: |
          poetry install
      - name: Run Update Rainfall
        run: poetry run python update_rainfall.py
      - name: Commit and Push
        run: |
          git config --local user.email "robot@koenvangilst.nl"
          git config --local user.name "Robot"
          git add .
          git commit -m "Update rainfall"
          git push
